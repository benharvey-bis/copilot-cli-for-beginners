[project]
name = "book-app"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["pytest", "prompt-toolkit"]

[tool.ruff]
target-version = "py310"
line-length = 88

[tool.ruff.lint]
extend-select = ["E", "F", "I"]
ignore = ["E501", "TRY004"]
